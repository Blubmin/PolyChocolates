CREATE TABLE [dbo].[PRODUCT_ENTRY]
(
	[PRODUCT_ID] INT NOT NULL PRIMARY KEY, 
    [RECIPE_ENTRY_ID] INT NOT NULL, 
    [LOT_CODE] VARCHAR(50) NOT NULL, 
    [AMOUNT_PACKAGED] INT NOT NULL, 
    [AMOUNT_PRODUCED] INT NOT NULL, 
    [QUALITY_ENTRY_ID] INT NOT NULL, 
    CONSTRAINT [RECIPE_ENTRY_KEY] FOREIGN KEY ([RECIPE_ENTRY_ID]) REFERENCES [ToTable]([ToTableColumn]), 
    CONSTRAINT [QUALITY_ENTRY_KEY] FOREIGN KEY ([QUALITY_ENTRY_ID]) REFERENCES [ToTable]([ToTableColumn])
)
